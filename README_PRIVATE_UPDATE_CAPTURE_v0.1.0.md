# Private repository update — Wonder Capture Companion v0.1.0-alpha

Upload this source package only to the private `FlowerKinPJBoi/Wonder-Codex-Importer` repository.

After replacing the repository source on `main`, GitHub Actions should automatically run **Build Wonder Capture Companion — Private Alpha** because the update adds `.github/workflows/build-capture-companion.yml` and the `capture-companion/` source tree.

The successful workflow artifact will be named:

`WonderCaptureCompanion-v0.1.0-alpha-win-x64`

Download and extract that artifact. The inner tester ZIP and SHA-256 file are suitable for the existing Wonder Codex private app vault after PJ reviews the workflow, dependency inventory, self-test, and read-only audit results.

Do not replace the current public Importer or Pegasus Transit vault entries with this new app. Wonder Capture Companion is a third, separate private-alpha application.
