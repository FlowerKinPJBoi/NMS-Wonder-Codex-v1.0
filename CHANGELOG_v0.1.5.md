# Wonder Codex Importer v0.1.5 — Clean-Room Pairing

- Adds a private matched-pair research panel.
- Lets a tester pair a raw WGS candidate with a decoded JSON export from the same slot.
- Derives provisional key mappings locally using structural order, value-kind compatibility, and repeated evidence.
- Applies the provisional mapping in memory and reruns the existing Wonder analyzer.
- Copies a redacted evidence report to the clipboard; no raw save values or paths are included.
- Adds synthetic self-tests for mapping derivation and in-memory key translation.
- Keeps the strict read-only save-file contract.
